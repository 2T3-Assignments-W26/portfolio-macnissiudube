git add. (to add files to the staging area)
git status. (To check the files or the status of the files)
git commit -m (to comment on the file)
git push oringin main (to push the file onto github)
git pull origin main (To pull the file from github)

git config --global core.editor "code --wait" 

To test it, stage the files: 

git add .  
git diff  (no output provided) 

git diff --staged (similar output to the above will be provided) 

$a = 123.45
$a
$a.GetType()

$a = "Patrick"
$a.GetType()

[int] $b = 123
$b.GetType()

$b = "Alex"

$colors = @("Orange", "Yellow", "Green", "Blue", "Indigo")

$colors.GetType() This is an object

$a=8..11
$a
$a.Gettype() This is also an object

get-date
$dateVariable = get-date
$dateVariable

get-date | get-member

$dateVariable.AddDays(3)

$dateVariable.AddDays(3) = $datePlus

$dateVariable.AddYears(-3) = $datePast

write-host "`$x + `$y= " $x + $y
$x + $y=  100 + 200

write-host "`$x + `$y= " ($x + $y)
$x + $y=  300

Modulo (%) check to see if a division has a remainder

$c = 100

if($c % 2 -eq 0){
>> Write-Host "Number is even"
>> }
Number is even


$i = 0
While ($i -lt 5)
>> {
>> "`$i equals $i. This is less than 5"
>> $i++
>> }

$i equals 0. This is less than 5
$i equals 1. This is less than 5
$i equals 2. This is less than 5
$i equals 3. This is less than 5
$i equals 4. This is less than 5